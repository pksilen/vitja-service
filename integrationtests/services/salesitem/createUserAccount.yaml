- tests:
    - testName: create user account
  testTemplate:
    serviceFunctionName: userAccountService.createUserAccount
    response:
      tests:
        - pm.collectionVariables.set('userAccountId', response._id)
    executeBefore: salesItemService.createSalesItem





