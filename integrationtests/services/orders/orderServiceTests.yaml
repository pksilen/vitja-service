- testTemplate:
    serviceFunctionName: ordersService.updateOrderItemState
    argument:
      newState: '{{newState}}'
    executeAfter: ordersService.updateOrderItemState
  tests:
    - testName: delivered
      testProperties:
        newState: delivered
    - testName: returning
      testProperties:
        newState: returning
    - testName: returned
      testProperties:
        newState: returned

- testTemplate:
    serviceFunctionName: salesItemsService.getSalesItem
    response:
      tests:
        - pm.expect(response.state).to.eql('sold');
    executeAfter: ordersService.placeOrder
  tests:
    - testName: state == sold

- testTemplate:
    serviceFunctionName: salesItemsService.getSalesItem
    response:
      tests:
        - pm.expect(response.state).to.eql('forSale');
    executeAfter: ordersService.updateOrderItemState
  tests:
    - testName: state == forSale
