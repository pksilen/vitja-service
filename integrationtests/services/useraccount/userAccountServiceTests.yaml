- testTemplate:
    serviceFunctionName: userAccountService.createUser
    argument:
      userName: test2@test.com
    response:
      tests:
        - pm.collectionVariables.set('followedUserId', response._id)
    executeAfter: userAccountService.getUser
  tests:
    - testName: followed user

