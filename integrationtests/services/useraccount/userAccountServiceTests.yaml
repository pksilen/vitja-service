- testTemplate:
    serviceFunctionName: userAccountService.createUserAccount
    argument:
      userName: test2@test.com
    response:
      tests:
        - pm.collectionVariables.set('followedUserAccountId', response._id)
    executeAfter: userAccountService.getUserAccount
  tests:
    - testName: followed user

- testTemplate:
    serviceFunctionName: salesItemService.createSalesItem
    response:
      tests:
        - pm.collectionVariables.set('salesItemId', response._id)
    executeBefore: userAccountService.addToFavoriteSalesItems


