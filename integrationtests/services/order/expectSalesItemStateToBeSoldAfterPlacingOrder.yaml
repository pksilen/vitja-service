- testTemplate:
    serviceFunctionName: salesItemService.getSalesItem
    response:
      tests:
        - pm.expect(response.state).to.eql('sold');
    executeAfter: orderService.placeOrder
  tests:
    - testName: expect sales item state to be sold
